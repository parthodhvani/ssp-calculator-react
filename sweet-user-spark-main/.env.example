# WordPress base URL (no trailing slash).
# Include the subdirectory if WP lives in one (e.g. /ssp-calculator).
VITE_WP_API_URL=https://devwp1.websiteserverhost.biz/ssp-calculator

# Preferred: Calculate page ID (your endpoint ends with /pages/130)
VITE_WP_CALCULATE_PAGE_ID=130

# Fallback only if PAGE_ID is not set (your page slug is calculator-page)
# VITE_WP_CALCULATE_SLUG=calculator-page
